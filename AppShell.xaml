<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Tokero.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Tokero.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="Tokero">

    <ShellContent Title="Login"
                  ContentTemplate="{DataTemplate local:LoginPage}"
                  Route="LoginPage" />

    <ShellContent Title="Dashboard"
                  ContentTemplate="{DataTemplate local:DashboardPage}"
                  Route="DashboardPage" />

    <ShellContent Title="Portfolio"
                  ContentTemplate="{DataTemplate local:PortfolioPage}"
                  Route="PortfolioPage" />

    <ShellContent Title="Investment Plan"
                  ContentTemplate="{DataTemplate local:InvestmentPlanPage}"
                  Route="InvestmentPlanPage" />

</Shell>
